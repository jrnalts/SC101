"""File: coin_flip_runs.pyName:-----------------------This program should simulate coin flip(s)with the number of runs input by users.A 'run' is defined as consecutive resultson either 'H' or 'T'. For example, 'HHHHHTHTT'is regarded as a 2-run result.Your program should stop immediately after yourcoin flip results reach the number of runs!"""import random as rdef main():    print('Let’s flip a coin!')    num_run = give_num_run()    result = origin = last = flip_coin()  # 紀錄第 1 次擲幣結果    continuous = 0  # 連續出現次數    while continuous != num_run:        current = flip_coin()        result += current  # 紀錄本次擲幣結果        if current == last:  # 與前 1 次相同            """            是第2次擲幣：相同代表開始連續            其他情況：若與前 2 次不同，但與前一次相同代表開始連續            """            if current != origin or len(result) == 2:                continuous += 1        # 把前兩個位置的紀錄重置（往前推移）        origin = last        last = current    print(result)def give_num_run():    num_run = str(input('Number of runs: '))    while not num_run.isdigit():        print('Please give a positive integer as num_run.')        num_run = str(input('Number of runs: '))    num_run = int(num_run)    return num_rundef flip_coin():    num = r.choice(range(2))    if num == 0:        return 'H'    elif num == 1:        return 'T'# ---- DO NOT EDIT CODE BELOW THIS LINE ---- #if __name__ == "__main__":    main()